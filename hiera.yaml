---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "OS Family & Version"
    path: "%{facts.os.family}-%{facts.os.release.major}.yml"

  - name: "common"
    path: "common.yml"
